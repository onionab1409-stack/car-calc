/**
 * TelegramProvider — обёртка для Telegram WebApp SDK
 *
 * Инициализирует SDK, предоставляет контекст:
 * - WebApp.ready()
 * - MainButton, BackButton
 * - HapticFeedback
 * - Тема Telegram (цвета)
 * - Данные пользователя
 *
 * Реализация: P6 · Фронтенд (подэтап 6.1)
 */

'use client';

// TODO: P6
export function TelegramProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
